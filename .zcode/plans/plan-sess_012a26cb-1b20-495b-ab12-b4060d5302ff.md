## 目标
把 `README.md` / `README_ZH.md` 从"内部项目备忘录"重写为"能被搜到、值得 star"的开源项目首页，同时忠实反映代码里真实存在的功能（当前 README 严重低估了项目能力——截图标注、OCR、全局搜索、通知镜像、iOS 支持、静态加密全部没写）。

## 修改的文件
1. **`README.md`**（英文，完整重写）——作为主 README，面向国际搜索流量。
2. **`README_ZH.md`**（中文，完整重写，与英文同步）。
3. **`LICENSE`**（新增，MIT）——见下方"待你确认的决定"。
4. `AGENTS.md` 对话记录区追加本次改动条目（按既有约定）。

## 待你确认的决定（在批准时可否一并说明）
- **许可证**：我建议新增 MIT `LICENSE` 文件并把 README 的 License 段改为 MIT（+ badge）。如果你要保留"内部项目/不开源"，回复"保留内部"即可，我会保留原 License 文案、不加 license badge，其余照做。MIT 对 star/传播最友好，是我的推荐项。

## 新 README 结构（中英双版本同步）

1. **Hero 区**（居中）
   - Logo（`Logo.png`）+ 标题 `Clipy`
   - 一句话定位，覆盖完整能力面：*"A native macOS menu-bar clipboard manager + screenshot tool, with encrypted LAN sync to your phone."*
   - 语言切换器 `中文 | English`
2. **Badges 行**（提升可信度与专业感，全部用 shields.io / GitHub Actions）
   - Release workflow 构建状态（`.github/workflows/release.yml`）
   - Platforms: `macOS 13+ · Android · iOS`
   - Language: `Swift / Flutter`
   - Latest Release（自动指向 Releases）
   - License: MIT（若你同意）/ 否则省略
   - Stars / Downloads
3. **Why Clipy / 卖点段**（2–3 行 elevator pitch，关键词密集以利于搜索）：clipboard history、snippets、screenshot annotation、on-device OCR、global search、phone-notification mirror、encrypted LAN sync、file transfer。
4. **Screenshots**
   - 现有 2 张（菜单栏 `res/menubar.png`、片段编辑器 `res/fragment1.png`）。
   - 为截图标注工具、全局搜索、通知镜像预留**带占位说明**的图位（你后续直接替换路径即可，无需改版式）。
5. **Features**（分组 + emoji，扫读友好；把目前被埋没的能力全部写出）
   - 📋 剪贴板历史：文本/RTF/HTML/PDF/图片/文件，SHA-256 去重+置顶，文件路径感知+Finder 定位，排除指定 App，可选静态加密（密钥存 Keychain）。
   - ✂️ 片段管理（macOS）：文件夹+片段、拖拽排序、全局快捷键录入器、XML 导入导出。
   - 📸 截图与标注（macOS，**重点新写**）：区域/窗口/全屏、矩形/箭头/文字/画笔/高亮/马赛克、放大镜、UI 元素自动吸附、贴图、**端侧 Vision OCR（中/英/自动）**、保存目录可配、全局快捷键（默认 ⇧⌘5）。
   - 🔍 全局搜索（macOS，**新写**）：⇧⌘F 呼出、正则、按类型/来源 App/日期筛选、结果排序、多选粘贴。
   - 🔄 局域网同步与文件传输：AES-GCM 256、Bonjour/mDNS 发现、128KB 分块单连接传输、实时进度、离线对端队列重投、环路防护（内容哈希）。
   - 🔔 通知镜像（Android→macOS，**新写**）：手机通知实时镜像到 Mac，双向 dismiss/clear、按 App 白名单。
   - 🔒 隐私：传输 AES-GCM 256 + 可选历史静态加密；**诚实标注**：当前预共享密钥为固定值，认证依赖用户勾选的授权设备列表。
   - ⌨️ 全局快捷键 + 🌍 中英双语 + 跨平台（macOS 原生 / Android & iOS Flutter）。
6. **Download**：直接链接 GitHub Releases（macOS zip / Android arm64 + armv7 APK），并说明 iOS 需自行构建。
7. **Build from source**：保留并精简现有 macOS/Android 构建说明（`./build_macos_app.sh` / `flutter build apk`），补 macOS 13+、Xcode CLI、TCC 权限（Accessibility/Screen Recording/Local Network）说明。
8. **Architecture**：精简版（主要 Manager 职责），保留可读性。
9. **Sync Protocol**：精简但保留关键技术点（发现/传输/分块/加密/授权/防环）。
10. **Project Structure**：目录速览。
11. **Contributing**：轻量段（欢迎 PR/Issue，构建前提）。
12. **License**：MIT（若同意）/ 内部项目（若保留）。
13. **Star History**（star-history.com 生成图，社区公认能有效提升 star 的收尾元素）。

## SEO / 可发现性增强（不改代码）
- 在 GitHub repo 的 Description 与 Topics 建议填入关键词：`clipboard-manager`, `macos`, `menu-bar`, `screenshot`, `ocr`, `snippets`, `lan-sync`, `flutter`, `appkit`, `android`。（这一步需你在 GitHub 网页操作，我会在交付时附清单。）
- README 关键词密集化、中英双版本，利于 GitHub/Google 检索。

## 执行约束
- 按 `AGENTS.md` 规则，执行 shell 前先 `proxy`。
- 所有功能描述基于 Explore agent 已核实的事实，不虚构能力；预共享密钥的安全局限如实标注。
- 不改动任何源码，只改 README/README_ZH、新增 LICENSE、追加 AGENTS.md 对话记录。